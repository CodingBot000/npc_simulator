# OpenAPI Contracts

Backend controller and DTO changes are promoted here as generated OpenAPI snapshots.

- `current.yaml`: latest promoted contract snapshot
- source of truth: Spring Boot backend controllers and DTOs
- frontend types/client generation should consume this directory after the API port phase
