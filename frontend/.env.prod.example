# Frontend production env
#
# Use as build-time/platform env for the frontend deployment.
# The URL must be reachable from the end user's browser.

VITE_API_BASE_URL=https://api.example.com
VITE_SHOW_INTERACTION_FAILURE_DEBUG=false
