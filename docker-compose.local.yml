services:
  frontend:
    image: node:22-alpine
    working_dir: /workspace
    command: >
      sh -lc "npm ci && npm run dev --workspace frontend -- --hostname 0.0.0.0"
    environment:
      NPC_SIMULATOR_ROOT: /workspace
      NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8080}
      LLM_PROVIDER_MODE: ${LLM_PROVIDER_MODE:-codex}
      LOCAL_REPLY_ADAPTER_MODE: ${LOCAL_REPLY_ADAPTER_MODE:-off}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-5.4}
      LOW_COST_MODEL: ${LOW_COST_MODEL:-gpt-5.4-mini}
      PREMIUM_MODEL: ${PREMIUM_MODEL:-gpt-5.4}
      LOW_COST_FALLBACK_MODEL: ${LOW_COST_FALLBACK_MODEL:-gpt-5.4-mini}
      PREMIUM_FALLBACK_MODEL: ${PREMIUM_FALLBACK_MODEL:-gpt-5.4-mini}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    volumes:
      - .:/workspace
      - frontend-node-modules:/workspace/node_modules
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    depends_on:
      - backend

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      BACKEND_PORT: ${BACKEND_PORT:-8080}
      BACKEND_STORAGE_ROOT: ${BACKEND_STORAGE_ROOT:-/srv/npc-simulator/storage}
      NPC_SIMULATOR_ROOT: /workspace
      NPC_SIMULATOR_BRIDGE_ENABLED: ${NPC_SIMULATOR_BRIDGE_ENABLED:-true}
      NPC_SIMULATOR_CORS_ALLOWED_ORIGINS: ${NPC_SIMULATOR_CORS_ALLOWED_ORIGINS:-http://localhost:3000,http://127.0.0.1:3000}
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-local}
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-npc_simulator}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-npc_simulator}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-npc_simulator}
    volumes:
      - .:/workspace
      - ./backend/storage:/srv/npc-simulator/storage
    ports:
      - "${BACKEND_PORT:-8080}:8080"
    depends_on:
      - postgres

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-npc_simulator}
      POSTGRES_USER: ${POSTGRES_USER:-npc_simulator}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-npc_simulator}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  frontend-node-modules:
  postgres-data:
